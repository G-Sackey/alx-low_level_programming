#include <studio.h>
/**
 * main - Entry point
 * My first program
 * Return 1 after running the program
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);
}
